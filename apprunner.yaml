version: 1.0
runtime: python3
build:
  commands:
    build:
    - pip install poetry
    - poetry config virtualenvs.create false
    - poetry install --no-dev
    - apt-get update
    - yes | apt-get install coinor-cbc
run:
  command: python app.py
  network:
    port: 8000